/*
function = Define code once, and use it many times.
           To perform some code, call the function name.
           in other word function is a set instruction 
           that perform specific task
*/

function doSomething(){
   document.getElementById("getin").innerHTML = "This also heading section you can design by using css and html"
}

