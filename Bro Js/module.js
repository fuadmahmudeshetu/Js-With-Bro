import * as  temp from "./math_util.js"

console.log(temp.temperature)

let celsius = temp.fahrenheitToCelsius(212);
console.log(celsius);

let fahrenheit = temp.celsiusToFahrenheit(100);
console.log(fahrenheit);

console.log(temp.PI);